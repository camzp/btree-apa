#include "b_tree.h"

int main()
{
    int qntNode;
    TNode *tree = init();
    printf("Digite a quantidade maxima de nós");
    scanf("%d", qntNode);
    int max[qntNode];
    int child[qntNode];
    int gChild[qntNode];
    return 0;
}